<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Mailgun</title>

    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body>
    <%= render 'header' %>
    <%= render 'flash' %>

    <section>
      <main>
        <%= yield %>
      </main>
    </section>

    <%= javascript_include_tag 'application' %>
  </body>
</html>
